/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['assets.aceternity.com'], // Add the hostname of the external image
      },

};

export default nextConfig;
